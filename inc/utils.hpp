#ifndef HELPER_HPP
#define HELPER_HPP

// Standard C++ Libraries
#include <iostream>
#include <string>

void printUsage(char** argv);
void printTitle();

std::string color(const std::string);

#endif // HELPER_HPP